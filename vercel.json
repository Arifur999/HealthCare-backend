{
  "buildCommand": "pnpm install && pnpm prisma generate && node scripts/fix-esm-imports.mjs && pnpm prisma migrate deploy",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "src/app/templates/**"
    }
  }
}
