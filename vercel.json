{
  "buildCommand": "pnpm install && pnpm prisma generate && pnpm prisma migrate deploy",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "src/app/templates/**"
    }
  }
}
